repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-added-large-files
# The gate on the repository's own Python, scoped to scripts/ so it never touches the .jinja
# templates or the rendered fixtures. It uses the config in pyproject.toml.
-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.1
    hooks:
    -   id: ruff
        files: ^scripts/
-   repo: https://github.com/psf/black
    rev: "26.5.1"
    hooks:
    -   id: black
        files: ^scripts/
-   repo: https://github.com/pre-commit/mirrors-mypy
    rev: v2.3.0
    hooks:
    -   id: mypy
        files: ^scripts/
        additional_dependencies: [types-PyYAML]
-   repo: https://github.com/compilerla/conventional-pre-commit
    rev: v4.0.0
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]
        args: [feat, fix, docs, style, refactor, tests, chore]
